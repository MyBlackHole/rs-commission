{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local bundled main window; only commission commands, no shell/filesystem/general HTTP plugin.",
  "windows": ["main"],
  "permissions": ["commission-bridge"]
}
